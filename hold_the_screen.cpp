#include <iostream>
using namespace std;


int main(){ 
    cout<<"This code is to hold the screen \n Press enter to exit >>>";
    cout.flush();
    cin.ignore(10000,'\n');
    // use double ignore  if previos code has a \n;


    return 0;
}